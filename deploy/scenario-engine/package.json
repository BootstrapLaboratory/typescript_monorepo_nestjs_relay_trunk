{
  "name": "deploy-scenario-engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "deploy-scenario-engine": "src/cli.mjs"
  },
  "scripts": {
    "build": "find src -name '*.mjs' -exec node --check {} \\;",
    "build:dev": "npm run build",
    "demo": "node src/cli.mjs demo",
    "lint": "eslint .",
    "test": "node --test test/*.test.mjs",
    "verify": "npm run test"
  },
  "dependencies": {
    "xstate": "^5.30.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "eslint": "^10.3.0"
  }
}
