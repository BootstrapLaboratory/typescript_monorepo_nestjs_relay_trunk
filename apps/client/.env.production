# Override these in the hosting provider with the real Cloud Run URL or
# custom domain when deploying the frontend.
VITE_GRAPHQL_HTTP=https://api.example.com/graphql
VITE_GRAPHQL_WS=wss://api.example.com/graphql
