{
    "name": "client",
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npm run build",
                "cwd": "apps/client"
            }
        },
        "start:dev": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npm run start:dev",
                "cwd": "apps/client"
            }
        },
        "lint": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npm run lint",
                "cwd": "apps/client"
            }
        }
    }
}
