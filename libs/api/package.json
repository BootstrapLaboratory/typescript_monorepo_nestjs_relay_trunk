{
  "name": "api-contract",
  "private": true,
  "version": "0.0.1",
  "files": [
    "schema.gql"
  ],
  "scripts": {
    "build": "node -e \"require('node:fs').accessSync('schema.gql')\"",
    "build:dev": "npm run build",
    "start:dev": "npm --prefix ../../apps/server run graphql:schema:watch"
  }
}
