name: force-deploy-server

on:
  workflow_dispatch:

permissions: {}

jobs:
  force-server-deploy:
    permissions:
      contents: write
      id-token: write
      packages: write
    uses: ./.github/workflows/ci-release.yaml
    with:
      force_targets_json: '["server"]'
    secrets: inherit
