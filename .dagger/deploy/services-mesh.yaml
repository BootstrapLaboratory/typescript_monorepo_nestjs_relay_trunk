# yaml-language-server: $schema=https://bootstraplaboratory.github.io/rush-delivery/schemas/v0.4.1/deploy-services-mesh.schema.json

services:
  server:
    deploy_after: []

  webapp:
    deploy_after:
      - server
